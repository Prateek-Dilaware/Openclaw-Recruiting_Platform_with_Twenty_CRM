# Schema V1 Archive

These scripts provisioned the original prototype CRM schema:

- `candidate`
- misspelled `requistion`
- `interview`

They are retained only as historical reference for migration and incident
analysis. They must **not** be run against CRM Schema V2. Use
[`../schema_v2/README.md`](../schema_v2/README.md) for the canonical deletion,
provisioning, seed, and verification sequence.