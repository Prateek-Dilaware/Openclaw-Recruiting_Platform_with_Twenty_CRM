# Examples

- **Availability:** Return three overlapping slots with ISO timestamps, original timezones, and conflict notes.
- **Schedule:** After approval, run the Interview Lifecycle workflow and report the workflow-run id plus any calendar action still required.
- **Cancel:** Request an approved cancellation workflow; do not directly set `interviewStatus` to `CANCELLED`.